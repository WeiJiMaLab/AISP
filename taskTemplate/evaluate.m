function evaluate
%function evaluate
% generates a csv file with a matrix of model evaluations over all models
% one line per model with columns [name], [Number of pars], [log-likelihood per
% subject]
% loads the right parameters, evaluates the right model and saves the
% likelihoods into the csv with a name per line
